import type { Duration } from './types';
/**
 * Parses the elapsed hours, minutes, seconds and milliseconds from the input
 * @param d duration in milliseconds
 * @returns
 */
export declare const parseDuration: (d: number) => Duration;
export declare const formatDuration: (d: Duration, showMs?: boolean) => string;
